# Cash availability provider configuration
# Keep the real .env file private and never expose these values to Vite.
API_PORT=8787
ATM_PROVIDER_URL=
ATM_PROVIDER_API_KEY=
ATM_PROVIDER_AUTH_HEADER=Authorization
ATM_PROVIDER_AUTH_PREFIX=Bearer
